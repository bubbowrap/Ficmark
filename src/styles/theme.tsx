import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#3539BD',
    },
    background: {
      default: '#191D25',
    },
  },
});

export default theme;
